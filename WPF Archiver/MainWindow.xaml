<Window x:Class="BestandsArchivering.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Bestands Archiver" Height="350" Width="600">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Bronmap -->
        <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
            <TextBox x:Name="txtSourcePath" Width="400" IsReadOnly="True" Margin="0,0,10,0"/>
            <Button Content="Selecteer bronmap" Click="BtnSelectSource_Click"/>
        </StackPanel>

        <!-- Doelmap -->
        <StackPanel Orientation="Horizontal" Grid.Row="1" Margin="0,0,0,10">
            <TextBox x:Name="txtTargetPath" Width="400" IsReadOnly="True" Margin="0,0,10,0"/>
            <Button Content="Selecteer doelmap" Click="BtnSelectTarget_Click"/>
        </StackPanel>

        <!-- Datum -->
        <StackPanel Orientation="Horizontal" Grid.Row="2" Margin="0,0,0,10">
            <Label Content="Datum:" VerticalAlignment="Center"/>
            <DatePicker x:Name="dpCutoffDate" Margin="10,0"/>
        </StackPanel>

        <!-- Actieknop -->
        <Button Grid.Row="3" Content="Verplaats bestanden" Click="BtnMoveFiles_Click" Padding="10" HorizontalAlignment="Left"/>

        <!-- Log output -->
        <ListBox Grid.Row="4" x:Name="lstLogs" Margin="0,10,0,0"/>
    </Grid>
</Window>
